package com.example.weatherapps.API

import com.example.weatherapps.Model.WeatherModel
import retrofit2.Call
import retrofit2.Response
import javax.inject.Inject

/*class ApiHelperImpl @Inject constructor(private val apiInterface: APIinterface) : ApiHelper {
    override suspend fun getCurrentWeather(
        lat: Double,
        lon: Double,
        appID: String
    ): Call<WeatherModel> {
        TODO("Not yet implemented")
    }

    override suspend fun getCityWeather(city: String, appID: String): Call<WeatherModel> {
        TODO("Not yet implemented")
    }


}*/